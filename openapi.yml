*** Begin Patch
*** Update File: openapi.yml
@@
   /api/profile/change-password/:
     post:
       operationId: changePasswordCreate
       tags:
         - Profile
       summary: Change password for the authenticated member
       description: Change password for the authenticated member
       security:
         - cookieAuth: []
+      requestBody:
+        required: true
+        content:
+          application/json:
+            schema:
+              type: object
+              required:
+                - old_password
+                - new_password
+              properties:
+                old_password:
+                  type: string
+                  description: Текущий пароль пользователя
+                  example: Oldpass123
+                new_password:
+                  type: string
+                  description: Новый пароль пользователя (минимум 6 символов)
+                  minLength: 6
+                  example: Newpass456
       responses:
         '200':
           description: Password changed successfully
+          content:
+            application/json:
+              schema:
+                type: object
+                required:
+                  - detail
+                properties:
+                  detail:
+                    type: string
+                    example: Пароль успешно изменен.
         '400':
           description: Invalid data supplied
+          content:
+            application/json:
+              schema:
+                type: object
+                properties:
+                  old_password:
+                    type: array
+                    items:
+                      type: string
+                  new_password:
+                    type: array
+                    items:
+                      type: string
+                  non_field_errors:
+                    type: array
+                    items:
+                      type: string
+              example:
+                new_password:
+                  - Новый пароль должен содержать минимум 6 символов.
*** End Patch
